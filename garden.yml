kind: Project
name: gdb-elk
environments:
  - name: dev
providers:
  - name: kubernetes
    environments: [dev]
    context: dev-gke
    namespace: gdb-elk
    defaultHostname: gdb-dev.ehealthafrica.org
    buildMode: cluster-docker
